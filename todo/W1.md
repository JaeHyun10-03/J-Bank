# W1 — 스캐폴딩·공통 기반과 고객·계좌 도메인

> 근거: `docs/07_J-Bank_구현계획.md` 4절, 9절, 11절
> 완료 기준: PR이 CI 통과 + ArchUnit 초록불 + 실명번호 암호문 저장 + 동일 실명번호 재등록 409 거부

## 백엔드 (`apps/jbank-api`)

### 공통 기반

- [x] `ApiResponse<T>` 제네릭 응답 봉투 클래스 (API설계 2.4절)
- [x] `ErrorCode` 열거형 — 공통 에러코드 표 (API설계 2.7절)
- [x] `@RestControllerAdvice` 전역 예외 처리기 — 모든 예외를 `ApiResponse` 포맷으로 변환
- [x] 금액 Jackson 커스텀 시리얼라이저 — 항상 문자열 직렬화, `BigDecimal` 고정
- [x] 요청추적ID 서블릿 필터 — 진단 컨텍스트에 심기
- [ ] Springdoc `/v3/api-docs`, Swagger UI 노출 확인

### 고객·계좌 도메인

- [ ] `Customer` 엔티티 + Flyway 마이그레이션 (ERD 2.1절)
- [ ] `Account` 엔티티 + Flyway 마이그레이션 (ERD 2.2절) — 지급정지 금액 컬럼 처음부터 포함
- [ ] AES-256-GCM `AttributeConverter` — 실명번호·연락처·주소 투명 암복호화
- [ ] HMAC-SHA256 해시 컬럼 — 실명번호 중복 확인용 (암호문으론 조회 불가)
- [ ] 계좌번호 채번 — 지점코드 3자리 + 일련번호 + 체크디지트, 모듈러스10, DB 시퀀스로 동시 채번 충돌 차단
- [ ] CDD 등급 산정 규칙기반 서비스 (직업·거래목적·실명확인방식 → 위험도)
- [ ] API-001 고객 등록
- [ ] API-002 계좌 개설
- [ ] API-003 계좌 상세 조회
- [ ] API-004 계좌 상태 변경
- [ ] API-005 계좌 해지
- [ ] API-013 강화된 고객확인 등록
- [ ] API-017 고객별 계좌 목록
- [ ] 단위 테스트 — 체크디지트 검증, CDD 등급 산정 로직

## 프론트엔드

없음. 구현계획 8절 — 프론트엔드 트랙은 W3부터 백엔드와 병행 시작. 스캐폴딩은 이미 끝남.

## 인프라 / 데브옵스

- [ ] `backend-ci.yml` 본격화 — ArchUnit 테스트 실행 단계, Spotless 포맷검사 단계 추가(지금은 compile만)
- [ ] OpenAPI 명세 스냅샷 드리프트 검사 CI 단계 — 덤프 결과 vs 커밋된 `contracts/openapi/openapi.yaml` 비교
- [ ] `infra/docker/jbank-api/Dockerfile` 작성
- [ ] `backend-cd.yml` — main 병합 시 이미지 빌드 후 레지스트리 푸시
- [ ] `scripts/generate-api.sh` 실동작 검증 (엔드포인트 생기면)

인프라 core 프로파일(PostgreSQL·Redis)은 이미 떠서 검증됨, 손댈 것 없음.
