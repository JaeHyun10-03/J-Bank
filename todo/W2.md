# W2 — 원장·거래 코어와 동시성·멱등성 검증

> 근거: `docs/07_J-Bank_구현계획.md` 4절(W2), 9절, ERD 문서 2.4절, 요구사항명세서 FR-TXN-002·FR-TXN-003
> 완료 기준: 이체가 복식부기로 기록되고 동시성 시나리오 다섯 가지 통과, 부하 테스트 베이스라인 기록

## 백엔드 (`apps/jbank-api`)

### 원장·거래 도메인

- [ ] `LedgerEntry`, `Transaction` 엔티티 + Flyway 마이그레이션
- [ ] 원장 수정·삭제 이중 방어 — 리포지토리에 수정/삭제 메서드 미노출 + 엔티티 리스너 `@PreUpdate`/`@PreRemove` 예외
- [ ] 거래 상태 기계 전체 정의 (ERD 2.4절) — 인증 대기 경로는 아직 없어도 상태 열거형·허용 전이 규칙은 전체 정의
- [ ] 이체 처리 절차 구현 (FR-TXN-003) — 멱등성 키 중복 확인 → 두 계좌 식별자 오름차순 정렬 후 그 순서로 비관적 쓰기 락 획득 → 양쪽 계좌 상태·출금 가능 금액 검증 → 차변·대변 원장 두 건 단일 트랜잭션 기록 → 거래 이력 저장
- [ ] 잔액 캐시 컬럼 트랜잭션 내 갱신 + 원장 엔트리별 반영 직후 잔액 스냅샷 기록
- [ ] 검증 기준을 잔액이 아닌 출금 가능 금액으로 고정 (지급정지 금액은 이번 주엔 항상 0)
- [ ] 멱등성 이중 방어 — 서비스 진입 시 거래 테이블 사전 조회 + DB 유니크 제약 위반 예외 캐치 후 원결과 반환
- [ ] API-006 입금
- [ ] API-007 출금
- [ ] API-008 이체
- [ ] API-009 잔액 조회
- [ ] API-010 거래내역 조회

### 동시성 검증

- [ ] Testcontainers 기반 통합 테스트 환경 구성 (H2 대신 실제 PostgreSQL·Redis)
- [ ] 시나리오① 잔액 10만원 계좌에 1만원 출금 100건 동시 요청 → 정확히 10건만 성공, 나머지 잔액부족 실패
- [ ] 시나리오② A→B·B→A 이체 각 50건 동시 요청 → 교착상태 없이 전부 처리, 두 계좌 잔액 합 보존
- [ ] 시나리오③ 동일 멱등성 키로 10건 동시 요청 → 거래 정확히 1건 생성, 10개 응답 모두 동일 거래식별자
- [ ] 시나리오④ 랜덤 계좌쌍 이체 1000건 → 전 계좌 원장합산=캐시잔액 일치, 전체 차변합=대변합
- [ ] 시나리오⑤ 이체 도중 강제 예외 발생 → 원장 두 건 모두 롤백, 한쪽만 남는 상태 없음
- [ ] ADR — 이번 주 분산 락(Redisson) 도입 보류 근거 기록 (단일 인스턴스에서는 DB 행 락으로 충분, 실제 필요 시점은 W7 배치 잡 단일 실행 보장)

## 프론트엔드

없음. 구현계획 8절 — 프론트엔드 트랙은 W3부터 백엔드와 병행 시작.

## 인프라 / 데브옵스

- [ ] `backend-ci.yml`에 Testcontainers 기반 통합 테스트 자동화 단계 반영
- [ ] k6 스크립트 작성 — 이체 엔드포인트 부하 테스트
- [ ] k6 베이스라인 측정 및 `perf/results`에 결과 커밋 — NFR-PERF-001 기준(이체 200ms 이내, 초당 100건) 대비 기록, 데이터 건수·인스턴스 수·컨테이너 자원 제한·떠 있는 Compose 프로파일 함께 기록
