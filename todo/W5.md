# W5 — 배치 처리와 고액이체 2차 인증

> 근거: `docs/07_J-Bank_구현계획.md` 5절(W5)·8절(프론트엔드 트랙)·9절(데브옵스 트랙),
> `docs/02_J-Bank_ERD.md` 2.7절(CtrReportQueue)·3절(부분 인덱스),
> 요구사항명세서 NFR-PERF-001·FR-TXN-002
> 완료 기준: 세 배치 잡이 재실행 안전하게 동작하고, 고액이체가 2차 인증
> 상태 기계를 거쳐 정상적으로 완료·취소되며 v0.2.0 태그를 붙인다

## 백엔드 (`apps/jbank-api`)

### 배치 — 이자 계산·만기 처리

- [x] 만기일 도래 계약 조회 → 이자 계산 → 원장 입금 엔트리 기록 → 계약 상태
      만기 전환 Step 구현
- [x] 이자 계산 소수점/원 단위 절사 규칙을 설정값으로 분리(코드 하드코딩 금지)

### 배치 — 원장 정합성 대사

- [x] 전 계좌 원장 합산 vs 캐시 잔액 비교, 전체 차변/대변 합 일치 검증
- [x] 불일치 발견 시 자동 보정 없이 리포트만 생성(로그로만 남김)

### 배치 — 고액현금거래(CTR) 판별

- [x] `CtrReportQueue` 엔티티 + Flyway 마이그레이션 (ERD 2.7절)
- [x] 일자별·고객별 현금 거래 합계 집계 → 기준 금액 초과 대상 적재
- [x] 실제 보고 전송은 로그 출력으로 대체(요구사항명세서 7.1절)

### 배치 공통

- [x] `batch` 패키지에 세 잡 구현, 실행은 프로파일+잡 이름 인자로만 분리
      (별도 모듈 아님) — 세 잡 모두 완료
- [x] 잡 파라미터에 기준일 포함, 동일 기준일 재실행 시 중복 처리 방지
      (이자 계산·정합성 대사는 Spring Batch JobInstance 식별 파라미터로 자체 차단,
      CTR 판별은 추가로 `(customer_id, account_id, transaction_date)` 유니크
      제약으로 DB 레벨에서도 차단)
- [x] 청크 크기는 기본값 유지(W6 측정 후 조정) — 이자 계산 잡 100건 고정

### 고액이체 2차 인증

- [ ] 임계 금액 초과 이체 → 인증 대기 상태 전이(W2에 정의한 상태 기계
      허용 전이만, 그 외 예외)
- [ ] OTP를 Redis에 3분 만료로 저장, 검증 실패 횟수 카운트 → 초과 시 거래 취소
- [ ] 인증 대기 상태 부분 인덱스(ERD 3절) 사용해 만료 대기 거래 정리
      스케줄러 구현
- [ ] 인증 대기 진입 시 지급정지 금액에 반영(원장은 건드리지 않음)
- [ ] 인증 성공 시 지급정지 해제 + 차변 원장 기록
- [ ] 실패/만료 시 원장은 그대로 두고 지급정지만 롤백
- [ ] API-016 2차 인증 검증 구현

### 마무리

- [ ] v0.2.0 태그

## 프론트엔드 (`apps/frontend`)

> 구현계획 8절. 잔액과 출금 가능 금액은 반드시 구분 표시(지급정지 때문에
> 다를 수 있음 — 잔액만 보여주면 출금 거절 사유를 사용자가 이해 못함).

- [ ] 입금 입력·확인 화면
- [ ] 출금 입력·확인 화면
- [ ] 이체 입력·확인 화면
- [ ] 2차 인증(OTP) 화면
- [ ] React Hook Form + Zod 폼 검증
- [ ] 서버 도메인 에러코드 → 폼 필드 에러 매핑 테이블(상수 파일 하나로 관리)

## 인프라 / 데브옵스

- [ ] 배치 잡 로컬 실행 확인(Compose 환경에서 프로파일+잡 이름 인자로 트리거)

## 요일별 진행 계획 (월 08/10 ~ 일 08/16)

- **월 08/10**: (이미 소진) 프론트엔드 인증·홈 화면 피그마 기준 재조정 —
  W4 잔여 작업, W5 배치 착수는 화부터
- **화 08/11**: `batch` 패키지 스캐폴딩, 이자 계산·만기 처리 잡(계산 규칙
  설정값 분리 포함)
- **수 08/12**: 원장 정합성 대사 잡, `CtrReportQueue` 마이그레이션 + 고액현금거래
  판별 잡
- **목 08/13**: 세 잡 재실행 안전성 검증(기준일 파라미터, 중복 실행 방지),
  고액이체 2차 인증 상태 전이 구현
- **금 08/14**: OTP Redis 저장·검증·실패 카운트, 만료 대기 거래 정리
  스케줄러(부분 인덱스), 지급정지 반영/해제/롤백 로직
- **토 08/15**: API-016 2차 인증 검증 구현, 백엔드 마무리 + v0.2.0 태그
- **일 08/16**: 프론트엔드 입출금·이체·2차 인증 화면 + 폼 검증/에러 매핑,
  `docs/devlog/` 기록
