# W6 — 관측 가능성·성능 최적화와 인프라 코드화

> 근거: `docs/07_J-Bank_구현계획.md` 6절(W6)·7절(성능 측정과 최적화 전략)·
> 8절(프론트엔드 트랙)·9절(데브옵스 트랙), 요구사항명세서 NFR-PERF-001,
> `perf/README.md`(성능 측정 규약)
> 완료 기준: 부하 테스트 개선 전후 수치가 Grafana 대시보드 캡처와 함께
> 기록되고, 인프라를 전부 파괴한 뒤 Terraform으로 재생성해도 동일하게
> 동작한다

## 백엔드 (`apps/jbank-api`) — 관측 가능성

- [x] Micrometer 지표 노출, Prometheus 스크레이프 연결
- [x] Grafana 대시보드: 이체 처리시간 백분위, 초당 처리 건수(TPS), 에러율,
      DB 커넥션 풀 사용률, 락 대기 시간
- [x] 로그 JSON 구조화 출력 전환(요청 추적 식별자는 W1부터 이미 있음 —
      포맷만 변경)
- [x] Loki 수집 연동

## 성능 최적화

> 진행 방식: 측정 → 가장 큰 병목 하나만 수정 → 재측정 → 결정 기록,
> 반복. 한 번에 여러 개 고치지 않는다(구현계획 7.5절).

- [x] W4에 미뤄둔 거래내역(API-010) 10만 건 EXPLAIN 벤치마크 실행·기록
      (`perf/README.md` "실행 대기" 절차 그대로 — 실행 중 시드 스크립트의 `LATERAL`
      상관관계 누락 버그를 발견해 함께 수정)
- [x] 이체 엔드포인트 k6 재측정 — W2 베이스라인 이후 공백(W3~W5) 메우고
      현재 상태 기준 1건 확보
- [x] 측정 결과로 병목 1건 이상 확정 → 수정 → 재측정 최소 1사이클
      (복합 인덱스 V13 반영 → 재측정 — 계좌 3개 로컬 규모에선 계획 불변,
      아래 결정 기록 참고. "수정"은 반영했으나 지금 데이터 규모에서 개선
      효과는 측정 안 됨, 인덱스 자체는 유지)
- [x] 후보군(조회 복합 인덱스 / JPA N+1 → 페치조인·엔티티그래프·프로젝션 /
      커넥션 풀 크기 / Redis 캐시 / 거래내역 오프셋→키셋 페이지네이션 /
      발신함 폴링 주기·배치크기 / Spring Batch 청크 크기 / 원장 월별
      파티셔닝 / 가상 스레드 / 로그 레벨) 중 실제 병목으로 확인된 것만
      반영 — 근거 없이 선제 적용 금지 (복합 인덱스만 반영, UNION 재작성은
      실측으로 18배 느려짐을 확인해 반려)
- [x] 트레이드오프 포함해 결정 기록 남김(무엇을 대가로 내줬는지까지)
      (`perf/README.md` "복합 인덱스 반영 후 재측정" 절)

## 인프라 / 데브옵스 — 코드화

- [x] Terraform 네트워크 모듈: 퍼블릭·애플리케이션·데이터·관리 4단 서브넷
- [x] Terraform 모듈: RDS, ElastiCache, ALB, WAF, ECR, EKS
      (ALB는 HTTP 리스너+고정 503 뼈대만, 도메인 생기면 HTTPS·실제
      타깃그룹 연결 추가 예정 — ponytail 주석 남김)
- [x] 원격 상태: S3 저장 + DynamoDB 잠금
      (`infra/terraform/bootstrap` 별도 스택, envs/dev backend를 s3로 전환)
- [x] 파이프라인: `terraform plan`만 자동, `apply`는 수동 승인 단계
      (`.github/workflows/infra-cd.yml`, GitHub Environment 리뷰어 설정은
      저장소 설정에서 수동으로 별도 필요)
- [x] 비용 통제용 워크스페이스 분리(상시구동 구성 vs 시연 구성)
      (`envs/dev/main.tf` — `terraform.workspace == "demo"` 분기로 RDS
      Multi-AZ·EKS 노드 수 축소, default/always-on은 기존 사양 유지)
- [x] 인프라 전체 파괴 후 Terraform 재생성 → 동일 동작 검증
      (demo 워크스페이스 기준 88개 리소스 destroy → 재apply, RDS/EKS/
      ElastiCache/ALB 전부 available·ACTIVE 확인, kubectl 접근까지
      재검증. 과정에서 RDS 16.4·EKS 1.30 버전 지원 종료와 EKS
      access entry 미등록 버그 2건 발견해 함께 수정)

## 프론트엔드 (`apps/frontend`)

- [x] 상품 화면(가입·조회)
      (seed 상품을 피그마 시안 기준 j-kids/j-farm으로 교체, 목록 화면
      GET /api/v1/products 실연동, J팜 가입신청·완료 화면 구현, 내
      가입상품 조회 화면 구현. J키즈는 기존 J키즈통장 온보딩 흐름
      유지로 스코프 제외)
- [x] Playwright E2E — 화면플로우차트 문서 7개 흐름 전부. 이체는 임계금액
      이하/초과 두 경로 + OTP 불일치/만료 두 실패 경로까지 포함
      (`apps/frontend/e2e/` 6개 spec, 13개 테스트 전부 통과 확인.
      계좌해지 화면이 미구현 상태였어서 같이 만듦. PRD_001/PRD_002·
      TXN_001 서버 에러, EDD 분기는 클라이언트에서 미리 막히거나
      조건 재현이 어려워 제외 — devlog 참고)
- [ ] Vercel 배포 연결, PR마다 프리뷰 배포
- [ ] 루트 디렉터리 `apps/frontend` 지정, 프론트엔드 경로 무관 커밋은
      빌드 스킵 조건 설정

## 요일별 진행 계획 (월 08/17 ~ 일 08/23)

- **월 08/17**: Micrometer/Prometheus 지표 노출, Grafana 대시보드,
  로그 JSON 구조화 + Loki 연동
- **화 08/18**: 거래내역 10만 건 EXPLAIN 벤치마크 실행·기록, 이체
  엔드포인트 k6 재측정(W5 상태 기준), 병목 후보 정리
- **수 08/19**: 병목 1건 수정 → 재측정 → 결정 기록(반복 1사이클)
- **목 08/20**: Terraform 네트워크·RDS·ElastiCache 모듈
- **금 08/21**: Terraform ALB·WAF·ECR·EKS 모듈, 원격 상태(S3+DynamoDB),
  plan/apply 파이프라인 분리
- **토 08/22**: 비용 통제 워크스페이스 분리, 인프라 전체 파괴 후
  재생성 검증
- **일 08/23**: 프론트엔드 상품 화면 + Playwright E2E 7흐름 + Vercel
  배포/프리뷰 연결, `docs/devlog/` 기록
