apiVersion: v1
kind: Service
metadata:
  name: {{ include "jbank-api.fullname" . }}
  labels:
    {{- include "jbank-api.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "jbank-api.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
