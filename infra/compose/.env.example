# EC2의 infra/compose/.env 로 복사해 값을 채운다(chmod 600). 저장소에는 절대 올리지 않는다.
# 키 생성 예: openssl rand -base64 32  (JWT_SECRET은 -base64 48)
DB_PASSWORD=
PII_ENCRYPTION_KEY=
RESIDENT_REG_NO_HASH_KEY=
JWT_SECRET=
GRAFANA_ADMIN_PASSWORD=
# backend-cd가 배포할 때 넘겨준다. 수동 실행 시 비우면 latest.
IMAGE_TAG=
