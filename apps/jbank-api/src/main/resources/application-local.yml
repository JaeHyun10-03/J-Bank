spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jbank
    username: jbank
    password: jbank
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092

logging:
  level:
    com.jbank: DEBUG
